{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Self-update and restart. Desktop only: the updater and process plugins are compiled out on mobile (see Cargo.toml), and a capability naming permissions from an absent plugin is a hard startup error.",
  "platforms": ["linux", "macOS", "windows"],
  "windows": ["main"],
  "permissions": [
    "updater:default",
    "process:allow-restart"
  ]
}
